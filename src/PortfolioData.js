import resume from "./assets/resume.pdf";

function getPortfolioData() {
  const portfolio = {
  "home": {
    "resume": resume
  },
  "projects": []
};
  return portfolio;
}

export default getPortfolioData;